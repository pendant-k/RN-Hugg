import { View, Text } from "react-native";
import React from "react";

const Ledger = () => {
    return (
        <View>
            <Text>Ledger</Text>
        </View>
    );
};

export default Ledger;
